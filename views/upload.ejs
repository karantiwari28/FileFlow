<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FileFlow - Upload</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f9;
            color: #333;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
        }

        .header h1 {
            font-size: 3rem;
            color: #4CAF50; /* Green color */
        }

        .header p {
            font-size: 1.2rem;
            color: #555;
        }

        .content {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .intro {
            text-align: center;
            margin-bottom: 30px;
        }

        .intro h2 {
            font-size: 2rem;
            color: #4CAF50;
        }

        .intro p {
            font-size: 1rem;
            color: #555;
        }

        .btn {
            background-color: #4CAF50;
            color: white;
            padding: 15px 30px;
            font-size: 1.2rem;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            margin-top: 20px;
        }

        .btn:hover {
            background-color: #45a049;
        }

        input[type="file"] {
            margin: 20px;
        }

        .result {
            text-align: center;
            margin-top: 30px;
        }

        .result h3 {
            color: #4CAF50;
        }

        .result a {
            color: #4CAF50;
            font-size: 1.2rem;
            text-decoration: none;
        }

        .result a:hover {
            text-decoration: underline;
        }

        footer {
            text-align: center;
            margin-top: 50px;
            font-size: 1rem;
            color: #888;
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Upload Your File</h1>
            <p>Get a shareable download link for your uploaded file.</p>
        </header>

        <div class="content">
            <form action="/upload" method="POST" enctype="multipart/form-data">
                <input type="file" name="file" required>
                <button type="submit" class="btn">Upload</button>
            </form>

            <% if (link) { %>
                <div class="result">
                    <h3>File uploaded successfully!</h3>
                    <p>Shareable link:</p>
                    <a href="<%= link %>" target="_blank"><%= link %></a>
                </div>
            <% } %>
        </div>

        <footer>
            <p>&copy; 2024 FileFlow. All Rights Reserved.</p>
        </footer>
    </div>
</body>
</html>
